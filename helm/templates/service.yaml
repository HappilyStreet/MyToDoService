apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app }}-service
  labels:
    env: PROD
spec:
  selector:
    project: {{ .Values.app }}
  ports:
    - name: {{ .Values.app }}-listener
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: {{ .Values.servicePort}}
  type: {{ .Values.nodetype }}